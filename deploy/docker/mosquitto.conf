# Mosquitto configuration for FamilyRobot
# Minimal dev config — NOT hardened for production

listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type all
